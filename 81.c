#include <stdio.h>

int main()
{ 
    int a,b,c,i,j;
    printf("Enter the num a & b:");
    scanf("%d%d",&a,&b);
    if(a>b)
    printf("diff: %d",a-b);
    else
    printf("diff:%d",b-a);
    
    

    return 0;
}
