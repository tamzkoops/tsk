#include<stdio.h>
#include<string.h>
void main()
{
    int num;
  char str[10];
  printf("Enter the num:");
  scanf("%d",&num);
    if(num>0)
  if(num%13==0)
  printf("Yes");
  else
  printf("No");
	return 0;

}
